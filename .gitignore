*.iml
.gradle
/local.properties
local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# Firebase — never commit credentials or device-specific config
google-services.json
app/google-services.json

# Windows artefact
nul
app/nul

# ADB debug log dumps
adb_*.txt

# Local developer build helper
build_deploy.ps1

# Cloud Function dependencies — regenerated by `npm install`
functions/node_modules/

# Android signing keystores — NEVER commit
*.jks
*.keystore

# Environment / secret files
*.env
.env.*
service-account*.json
*-service-account.json
sa-key.json
**/sa-key.json

# Firebase service account keys — NEVER commit
app/src/main/assets/service_account.json
functions/sa-key.json
set-admin-claim.js
functions/set-admin-claim.js

# Java crash logs
hs_err_pid*.log

# Build output log
build_output.txt

# SQLite database files — runtime data, never commit
*.db

# SQLite base64 export dumps
*_b64.txt

# Gradle Wrapper JAR
gradle/wrapper/gradle-wrapper.jar

# IntelliJ/Android Studio configuration files
.idea/*.xml

# Gradle properties — contains machine-specific paths
gradle.properties
